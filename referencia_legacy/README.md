# Referência do motor Python anterior

Estes arquivos **não são executados pelo Netlify**. Foram preservados apenas para auditoria e comparação das regras durante a migração.

A versão online nova usa **Netlify Functions + RPCs SQL no Supabase**, sem Render.
